#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

// Lee los archivos que se encuentran en la carpeta seleccionada
/*
fs.readdir('/Mandy/Documents/JavaScript/Proyectos de programacion/PROYECTOS/Clases Front end/scl-2018-01-FE-markdown/README.md', (err, files) => {
  if (err) throw err;
  console.log(files);
  for (let i = 0; i < files.length; i++) {
    if (path.extname(files[1]) === '.md') {
     console.log(files[1]);
    }
  }
});
*/
